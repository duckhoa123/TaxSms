<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vnnet.kpi.web.persistence.SysSmsTemplateExMapper">
    <resultMap id="BaseResultMap" type="com.vnnet.kpi.web.model.SysSmsTemplate">
        <id column="template_id" jdbcType="INTEGER" property="templateId"/>
        <result column="sms_template" jdbcType="NVARCHAR" property="smsTemplate"/>
        <result column="program_id" jdbcType="BIGINT" property="programId"/>
        <result column="del_flag" jdbcType="INTEGER" property="delFlag"/>
        <result column="create_by" jdbcType="VARCHAR" property="createBy"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="last_update_by" jdbcType="VARCHAR" property="lastUpdateBy"/>
        <result column="last_update_time" jdbcType="TIMESTAMP" property="lastUpdateTime"/>
        <result column="program_name" jdbcType="NVARCHAR" property="programName"/>
    </resultMap>

    <select id="findPage" resultMap="BaseResultMap">
        select a.*, b.program_name from sys_sms_template a join sys_program b on a.program_id = b.program_id
        where b.del_flag = 0
        and ((#{fromDate} is null and #{toDate} is null) or (a.create_time >= #{fromDate} and a.create_time &lt;
        #{toDate}))
        and (a.program_id = #{programId} or (#{programId} = '0' and b.group_code = #{group}) or (#{programId} = '0' and
        #{group} = 'ALL'))
        order by a.program_id
    </select>

    <select id="findPageWithOutDate" resultMap="BaseResultMap">
        select a.*, b.program_name from sys_sms_template a join sys_program b on a.program_id = b.program_id
        where b.del_flag = 0
        and (a.program_id = #{programId} or (#{programId} = '0' and b.group_code = #{group}) or (#{programId} = '0' and
        #{group} = 'ALL'))
        order by a.program_id
    </select>


</mapper>
